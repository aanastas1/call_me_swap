main.c
(
    static void	context_init(t_context *context);
    void	put_error_n_exit(void);
    static void	free_all(t_context *context);
    void print_stack_a_b(t_context *context); - for debuging
    int	main(int argc, char **argv)
)

parse_args.c
(
  static double	compute_disorder_values(const int *values, int n);
  static int	ft_strcmp(const char *s1, const char *s2);
  static int	is_strategy_flag(char *src, t_context *context);
  static int fill_the_buffer(int max_args, char *arg, int **out_values, int *size)
  void	parse_args(int argc, char **argv, t_context *context);
)

complex_string_split.c
(
int complex_string_split(int **out_values, char *str);
 int	find_buf_size(char **buf);
 static int	add_nbr_to_arr(int **out_values, char *nptr, int idx);
 static void free_split(char **buf)
    int	is_number(char *src);
)

assistant_functions.c
(
void *xalloc(size_t count, size_t elem_size);
int out_of_int_range(long nbr);
long	ft_atoil(const char *nptr);
int out_of_int_range(long nbr);
int has_spaces(char *src);

)

prepare_stacks.c
(
    static void	stacks_init(t_arr *arr, int capacity);
    int	validate_no_dups(int *values, int n);
    
    void	compute_ranks(t_context *context, const int *values, int size);
    void	setup_stacks(t_context *context, int *values, int size);
)
